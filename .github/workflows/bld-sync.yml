# Generated by bld - DO NOT EDIT

name: bld-sync

on:
  schedule:
    - cron: '0 0 1 * *' # First day of each month
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Update bld
        run: |
          cd .bld
          go get -u github.com/fredrikaverpil/bld@latest
          go mod tidy

      - name: Generate workflows
        run: go run ./.bld update

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: 'chore: sync bld'
          title: 'chore: sync bld'
          body: |
            Automated update from bld.

            This PR updates:
            - `.bld/go.mod` (bld dependency)
            - `.github/workflows/`
          branch: chore/bld-sync
          delete-branch: true
