# Generated by sage-ci - DO NOT EDIT

name: sage-ci-sync

on:
  schedule:
    - cron: '0 0 1 * *' # First day of each month
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Update Sage
        run: make update-sage

      - name: Update sage-ci
        run: make update-sage-ci

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          commit-message: 'chore: sync sage-ci'
          title: 'chore: sync sage-ci'
          body: |
            Automated update from sage-ci.

            This PR updates:
            - `.sage/go.mod` (sage-ci dependency)
            - `.github/workflows/`
            - `Makefile`
          branch: chore/sage-ci-sync
          delete-branch: true
